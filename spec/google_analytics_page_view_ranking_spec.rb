require 'spec_helper'

describe GoogleAnalyticsPageViewRanking do
  it 'has a version number' do
    expect(GoogleAnalyticsPageViewRanking::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
